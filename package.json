{
  "name": "ian-milkowski-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs && node scripts/check-motion.mjs",
    "package": "npm run build && npm run check && node scripts/package.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
